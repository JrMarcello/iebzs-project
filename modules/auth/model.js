'use strict'

import user from './schema';

// module.exports.create = (data, callback) => {        
//     //new Schema(data).save(callback);
// };

export default {
    login: (data, callback) => {
        
    }
}
